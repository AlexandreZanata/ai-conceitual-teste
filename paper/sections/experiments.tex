\section{Experiments}
\label{sec:experiments}

\subsection{Wave chronology (archival)}
\begin{description}
  \item[W--Y] Tip and recipe discovery; PFB family promote; STREAM/GENCACHE/naive CTX kill.
  \item[Z] Interactive HITL: raw PFB decode fails (mean $\approx$1.0); wrap LOOKUP passes
        (mean 9.0). Doctrine: \textbf{PFB $\neq$ interactive LM}.
  \item[AA--AF] Product packaging: wrap bank, semantic wrap, apps, deploy docs;
        ship claim \textbf{AF packaged stack}.
  \item[AG--AP] Context/speed/app letter packs; generative arms repeatedly HOLD
        (ablated peak-only lift). Caution: letter clones without new holes are
        engineering, not discovery.
  \item[AQ--AS] Product trust: paraphrase hit, adversary FP, latency, KB coverage,
        mode UI, abstain.
  \item[AT--AZ] Ablated DECODE STRICT; held-out intent harden; over-refuse fix;
        NANOGEN8--10 DEFER.
  \item[BA] Forever anti-FP (BA-FOREVER), prod-path speed, content-first context,
        NANOGEN11 DEFER; live battery 10/10 with generative unlock locked.
\end{description}

\subsection{Primary boards cited in this paper}
\begin{itemize}
  \item Tip scoreboard (STAG$'$, EARLY, POOL) --- Table~\ref{tab:tips}.
  \item Product anti-FP (REALGAIN / AZ hold) --- Table~\ref{tab:antifp}.
  \item Production latency (FASTREAL) --- Table~\ref{tab:latency}.
  \item Generative status (NANOGEN6--11) --- Table~\ref{tab:gen}.
  \item Kill archive --- Table~\ref{tab:kills}.
\end{itemize}

\subsection{Reproduce (selected)}
\begin{verbatim}
npm run nano:z:ask -- --wrap --semwrap --question "..."
npm run nano:realgain
npm run nano:ba:fastreal
npm run nano:ba:ctxreal2
npm run nano:nanogen11
npm run nano:ba:real-eval
npm run nano:az:freeze
\end{verbatim}
Artifacts: \texttt{results/nano-lm/wave-*/}. Contracts: \texttt{nano\_lm/tests/}.
Formal notes: \texttt{docs/results/nano-lm/formal-*.md}.
