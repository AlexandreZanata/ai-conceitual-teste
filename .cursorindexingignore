# Indexing-only exclude — files stay @-mentionable but do not bloat the index.
#
# The 100-entry catalogue is a lookup table read by ID, not prose to be searched.
# Keep it @-mentionable; keep it out of semantic search results.

docs/hypotheses/CATALOG-*.md

# Generated and heavy
**/*.pdf
**/*.json
!results/**/summary.json
