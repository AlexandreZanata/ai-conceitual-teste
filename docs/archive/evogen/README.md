# Archived — EvoGen survival PoC (frozen)

> **Active lab = nano generative LM only** (`nano_lm/`, `docs/NANO-*`).  
> This tree is the closed C++ genetic / survival benchmark narrative (phases 02–09).

| Path | Content |
|------|---------|
| `plano-conceitual-evogen.md` | Original research intent (PT) |
| `ARCHITECTURE.md` / `GLOSSARY.md` | C++ layered PoC |
| `EXPERIMENTAL-DESIGN.md` | A/B/C + TB-* survival protocol |
| `results/` | Benchmark report, ADR, curves, summary |
| `use-cases/` | UC-001…003 |

**Code still in repo (frozen, not active research):** `src/`, `web/`, `experiments/`, `scripts/*survival*`, `CMakeLists.txt`.  
Do not extend survival hypotheses unless the lab book explicitly re-opens them.
